# -*- coding: utf-8 -*-
from odoo import http, exceptions

#
# class complaint_controller(http.Controller):
#
#     @http.route(['/resolved_controller'], type='http', auth='public', website=True)
#     def resolve(self, redirect=None, **post):
#         for i in self:
#             if i.status == 'resolved':
#                 raise exceptions.ValidationError("the selected must not have resolved status")

# Put your backend operations here, e.g.
# sale_model = request.env['sale.order']
# sale_model.create({...})

# After you're done, use either request.render() or request.redirect() to show
# a different page to the user. You can check out the website_portal module for